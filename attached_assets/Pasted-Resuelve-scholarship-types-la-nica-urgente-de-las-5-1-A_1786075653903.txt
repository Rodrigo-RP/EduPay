Resuelve scholarship_types, la única urgente de las 5:

1. Aplica el mismo patrón que ya usaste para payment_rules: crea la tabla
   con SQL directo contra la conexión real, con las columnas exactas que ya
   define shared/schema.ts, incluyendo el bloque de rollback documentado
   desde el principio (no como corrección posterior).

2. Reproduce el bug antes del fix si todavía puedes (revirtiendo
   temporalmente o confirmando con la ausencia de la tabla que ya
   verificaste): GET /api/scholarships y GET /api/admin/admissions-report
   deben mostrar la lista vacía. Después del fix, con datos reales
   insertados, deben mostrar la distribución real.

3. Investiga cómo estos dos endpoints manejan hoy el error de tabla
   inexistente: ¿devuelven la lista vacía porque atrapan la excepción de
   Postgres y la descartan silenciosamente (mismo patrón de catch vacío que
   ya corregimos varias veces esta sesión), o por alguna otra razón? Si es
   un catch silencioso, corrígelo para que un error real de base de datos
   se registre (log de error, no se trague), aunque la tabla ya exista
   ahora — la próxima tabla faltante no debería volver a esconderse así.

4. Ya que estos dos endpoints nunca llegaron a auditarse por estar rotos,
   aplícales ahora el mismo guard de rol que corresponda (revisa qué
   módulo de permissions.ts es el adecuado para consulta de becas —
   probablemente SCHOLARSHIPS.READ, que ya existe) antes de darlos por
   completos.

5. Deja constancia en el código o en un comentario de que
   scholarship_benefits, scholarship_criteria, platform_profiles y
   platform_subscriptions son schema anticipado sin ningún endpoint
   consumidor — no requieren acción hasta que alguien construya la
   funcionalidad que las use.

Prueba de ciclo completo: crear un scholarship_type real, confirmar que
aparece en ambos endpoints con los datos correctos.

Reinicia el servidor, confirma con las tres marcas de tiempo reales, corre
la suite completa, y pégame el resultado sin resumir.