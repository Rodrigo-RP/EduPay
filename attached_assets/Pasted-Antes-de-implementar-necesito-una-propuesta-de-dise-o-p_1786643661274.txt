Antes de implementar, necesito una propuesta de diseño para los dos
endpoints (creación manual + import masivo de familias), no código todavía:

1. Lógica compartida: dado el riesgo de divergencia que ya identificamos
   con storage.createStudent vs. la versión inline de import, propón cómo
   evitarlo aquí desde el diseño — idealmente ambos endpoints (manual y
   masivo) llaman a la misma función central de "crear/vincular familia",
   no dos implementaciones paralelas.

2. Orden de las piezas: dado que ya existe un import de estudiantes por
   separado, ¿el import de familias asume que los alumnos YA existen (los
   busca por CURP/id_referencia, igual que hace el import de becas), o
   también puede crear alumnos nuevos en el mismo paso? Dame tu
   recomendación.

3. Estructura del CSV para el import masivo: dado que una familia puede
   tener varios alumnos y varios tutores, ¿cómo se agrupan las filas de
   una misma familia en un archivo plano? Propón el formato de columnas
   (por ejemplo, una fila por combinación alumno-tutor con una clave de
   agrupación compartida) y explica por qué.

4. Deduplicación de tutores: dado que confirmamos que hoy un guardian
   puede pertenecer a varias familias sin restricción, y que el mismo
   tutor podría aparecer en varias filas del CSV (dos hijos, mismo padre),
   ¿el import debe buscar primero si ya existe un guardian con ese
   email/CURP antes de crear uno nuevo? ¿Qué pasa si encuentra una
   coincidencia parcial (mismo email, distinto teléfono)?

5. Endpoint manual: ¿qué forma debería tener el body de
   POST /api/admin/families para crear una familia con sus tutores y
   vincular alumnos ya existentes en una sola llamada?

6. guardian_id_principal: dado que hoy es puramente informativo, ¿el
   import/creación manual debería empezar a darle algún efecto real (por
   ejemplo, ser el destinatario por defecto del magic link si no se
   especifica otro), o lo dejamos como está por ahora y resolvemos eso en
   otro momento?

Solo el diseño con tu recomendación en cada punto — no implementes nada
todavía.