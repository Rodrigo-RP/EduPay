Implementa la tarea #163: Validar y Simular con lógica real, tal como se
detalló y aprobó.

1. GET /api/admin/configuracion/validacion-onboarding — 4-6 queries de
   integridad sobre el campus del JWT:
   - Alumnos sin familia asignada (sin fila en family_students)
   - Familias sin correo_institucional_familiar válido en ningún tutor
   - Becas asignadas a un alumno que no existe en students del campus
   - Adeudos migrados con monto_base_centavos = 0
   Devuelve { errores: [...], warnings: [...], ok: boolean }. Los errores
   son bloqueantes para el wizard, los warnings no.

2. GET /api/admin/configuracion/simulacion-cargos — cruza students ×
   concepts del campus para proyectar los cargos que generaría el batch
   automático. Devuelve { total_alumnos, total_cargos_proyectados_centavos,
   desglose_por_concepto: [...] }. No escribe nada en DB, es puramente de
   lectura/cálculo.

3. Guard SETTINGS.CONFIGURE en ambos, consistente con el resto de
   endpoints de configuración de onboarding.

4. Frontend (configuracion-inicial.tsx): reemplaza el PlaceholderStep de
   "validar" por un componente que llama al endpoint y muestra errores
   (bloqueantes, deshabilitan "Confirmar y continuar") y warnings (no
   bloqueantes, se muestran pero no impiden avanzar). El paso "simular"
   siempre permite continuar — es informativo.

5. Si simulación detecta que no hay ningún concepto configurado en el
   campus (total_cargos_proyectados_centavos = 0 por ausencia total de
   concepts, no por falta de alumnos), muestra el aviso con enlace a
   Ajustes Institucionales que ya acordamos, en vez de solo mostrar un
   cero sin contexto.

6. Pruebas, verificadas en DB/respuesta real, no solo código HTTP:
   - Campus con un alumno sin familia → error específico en la lista,
     ok=false
   - Campus con todos los datos correctos → ok=true, sin errores
   - Simulación con conceptos y alumnos reales → total proyectado
     matemáticamente correcto (verificar el cálculo, no solo que responda)
   - Campus sin ningún concepto → simulación devuelve 0 con el contexto
     correcto para mostrar el aviso, no un error

Reinicia el servidor, confirma con las tres marcas de tiempo reales, corre
la suite completa, y pégame el resultado sin resumir.